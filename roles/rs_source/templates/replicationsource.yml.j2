apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: rs-{{ repsrc.repdest.metadata.name }}
  namespace: {{ repsrc.repdest.metadata.namespace }}
  labels:
    "dr/managed": "true"
spec:
  sourcePVC: {{ repsrc.repdest.metadata.name }}
  trigger:
    schedule: "{{ rs_schedule }}"
  rsyncTLS:
    copyMethod: {{ copy_method }}
    keySecret: {{ volsync_tls_secret_name }}
    address: {{repsrc.result.status.rsyncTLS.address}}
